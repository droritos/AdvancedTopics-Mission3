# Execution Summary
- **Verification Commands Run**: Unity script and shader compilation check.
- **Summary of Changes**: 
  - Nuked the legacy `OnRenderImage` logic.
  - Created a completely proper `ScriptableRendererFeature` called `DoodleRenderFeature`.
  - Converted the doodle screen-shake shader into pure URP `HLSLPROGRAM` utilizing `Blit.hlsl`.
  - Exposed the `Material` reference directly to the inspector to prevent runtime lookup overhead via `Shader.Find`.
- **Follow-ups**:
  - The user must create a new Material using the `Hidden/URP/DoodleWobble` shader.
  - The user must assign this Material into their Universal Renderer Data asset under the new "Doodle Render Feature".
- **Manual validation steps**:
  - Assign the material and hit Play. The full screen will wobble dynamically via URP Post-Processing!
